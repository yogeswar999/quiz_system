spring.application.name=quiz-management-system
server.port=${PORT:8080}

# ---------- Railway MySQL ----------
spring.datasource.url=jdbc:mysql://mysql.railway.internal:3306/railway?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=VtGTRcqlEvhIzsekvbjcbmuzKaAFKVhu
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ---------- JPA / Hibernate ----------
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.sql.init.mode=never

# ---------- JWT ----------
jwt.secret=ThisIsADevOnlySecretKeyForJWTSigning_PleaseChangeInProduction_MinLength32
jwt.expiration.ms=86400000

# ---------- Default Admin ----------
app.default-admin.username=admin
app.default-admin.password=admin123

# ---------- CORS ----------
app.cors.allowed-origin=https://quizbattle-ce44f.web.app